---
- name: Verify master initialization
  hosts: all
  tasks:
    - name: Check if admin.conf exists
      ansible.builtin.stat:
        path: /etc/kubernetes/admin.conf
      register: admin_conf

    - name: Assert master is initialized
      ansible.builtin.assert:
        that:
          - admin_conf.stat.exists
